http://youtube.com - it's a domain name which associates (under the hood) with host API adress 172.101.4.192

For local development (client and server) on the one machine we use such domain name as localhost:3000 which under the hood is associated with host API address 127.0.0.1:3000