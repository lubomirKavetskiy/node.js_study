http://youtube.com - it's a domain name which associates (under the hood) with host IP adress 172.101.4.192

For local development (client and server) on the one machine we use such domain name as localhost:3000 (where 3000 - it's a PORT) which under the hood is associated with host IP address 127.0.0.1:3000 (where 127.0.0.1 - it's a loopback IP and 3000 - it's a PORT)
